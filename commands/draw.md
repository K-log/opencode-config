---
description: Open an interactive ASCII drawing canvas.
---

Call the `draw_ascii` tool immediately. If the user specified dimensions (e.g. `/draw 80 30` means width=80, height=30), pass them as `width` and `height`. Otherwise omit for defaults (60×20). No preamble.
